<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}LET'S EAT!!!{{else}}ANOTHER ONE PLEASE!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">GROSS!! TRASH IT</button>
</li>